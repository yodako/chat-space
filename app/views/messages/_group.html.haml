-# #aside-group--name group name
-# #aside-group--message new message
- current_user.groups.each do |group|
  = link_to group_messages_path(group) do
    #aside-group--name
      = group.name
    #aside-group--message
      メッセージはまだありません。
